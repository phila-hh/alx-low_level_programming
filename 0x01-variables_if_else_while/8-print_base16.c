#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 (Success)
 */
int main(void)
{
	for (int n = 48; n < 58; n++)
		putchar(n);
	for (int n = 97; n < 103; n++)
		putchar(n);
	putchar('\n');

	return (0);
}
